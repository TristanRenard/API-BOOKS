PORT=3000

# S3 Configuration (compatible avec AWS S3, Minio, etc.)
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=book-covers
S3_REGION=auto
S3_FORCE_PATH_STYLE=true

# URL publique pour accéder aux fichiers (optionnel)
# Si vide, utilisera l'endpoint S3 par défaut
S3_PUBLIC_URL=http://localhost:9000